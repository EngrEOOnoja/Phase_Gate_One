def	get_array_numbers(names):
	new_list = []
	if len(new_list) == 0:
		return "no one likes this"
	elif len(new_list) == 1:
		return names[0] " likes this"
	elif len(new_list) == 1:
		return names[0], " names[1] likes this"
	else:
		names[0], names[1] " and 2 others likes this ";
	
		